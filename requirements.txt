# ClearMind - 从零训练并发布大语言模型
# Core
torch>=2.1.0
sentencepiece>=0.2.0

# HF Tokenizer + chat_template + 发布生态
# transformers 锁 <5：5.x 与 4.x 在 chat_template / from_pretrained 序列化等处不向后兼容；
# 上线前在 AutoDL 镜像内固化此版本，避免本地 4.x 跑通、远程 5.x 翻车
transformers>=4.40.0,<5
jinja2>=3.1.0
safetensors>=0.4.0

# Data
datasets>=2.16.0
tqdm>=4.66.0

# Dataset 下载（download_data.py 用）
modelscope>=1.13.0
huggingface_hub>=0.20.0

# Config
pyyaml>=6.0.0

# Evaluation & Visualization
numpy>=1.24.0
matplotlib>=3.8.0

# Training observability（main/plus.yaml 默认 use_tensorboard: true）
tensorboard>=2.16.0

# Dev / preflight（preflight.sh 第 8 步会跑 pytest，缺这两个 preflight 直接报 ERROR）
pytest>=7.0.0
ruff>=0.4.0
